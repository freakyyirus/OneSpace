# OneSpace – copy to .env and fill in values
# Required for database (Prisma migrate + seed)
DATABASE_URL="postgresql://user:password@localhost:5432/onespace"

# Required for NextAuth (auth/login, auth/signup)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-long-random-secret"

# Optional: Google OAuth
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""
